import styles from "./ProfileButton.module.css";
import { useState } from "react";

const ProfileButton = () => {
  return <button className={styles.profileButton}></button>;
};

export default ProfileButton;
